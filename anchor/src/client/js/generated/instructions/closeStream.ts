/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { CASCADE_PROGRAM_ADDRESS } from '../programs';
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from '../shared';

export const CLOSE_STREAM_DISCRIMINATOR = new Uint8Array([
  255, 241, 196, 212, 95, 93, 160, 89,
]);

export function getCloseStreamDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLOSE_STREAM_DISCRIMINATOR
  );
}

export type CloseStreamInstruction<
  TProgram extends string = typeof CASCADE_PROGRAM_ADDRESS,
  TAccountEmployer extends string | AccountMeta<string> = string,
  TAccountStream extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends
    | string
    | AccountMeta<string> = 'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA',
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountEmployer extends string
        ? WritableSignerAccount<TAccountEmployer> &
            AccountSignerMeta<TAccountEmployer>
        : TAccountEmployer,
      TAccountStream extends string
        ? WritableAccount<TAccountStream>
        : TAccountStream,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseStreamInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseStreamInstructionDataArgs = {};

export function getCloseStreamInstructionDataEncoder(): FixedSizeEncoder<CloseStreamInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_STREAM_DISCRIMINATOR })
  );
}

export function getCloseStreamInstructionDataDecoder(): FixedSizeDecoder<CloseStreamInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseStreamInstructionDataCodec(): FixedSizeCodec<
  CloseStreamInstructionDataArgs,
  CloseStreamInstructionData
> {
  return combineCodec(
    getCloseStreamInstructionDataEncoder(),
    getCloseStreamInstructionDataDecoder()
  );
}

export type CloseStreamAsyncInput<
  TAccountEmployer extends string = string,
  TAccountStream extends string = string,
  TAccountVault extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  employer: TransactionSigner<TAccountEmployer>;
  stream: Address<TAccountStream>;
  vault?: Address<TAccountVault>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export async function getCloseStreamInstructionAsync<
  TAccountEmployer extends string,
  TAccountStream extends string,
  TAccountVault extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof CASCADE_PROGRAM_ADDRESS,
>(
  input: CloseStreamAsyncInput<
    TAccountEmployer,
    TAccountStream,
    TAccountVault,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): Promise<
  CloseStreamInstruction<
    TProgramAddress,
    TAccountEmployer,
    TAccountStream,
    TAccountVault,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? CASCADE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    employer: { value: input.employer ?? null, isWritable: true },
    stream: { value: input.stream ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(expectAddress(accounts.stream.value)),
      ],
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.employer),
      getAccountMeta(accounts.stream),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getCloseStreamInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseStreamInstruction<
    TProgramAddress,
    TAccountEmployer,
    TAccountStream,
    TAccountVault,
    TAccountTokenProgram
  >);
}

export type CloseStreamInput<
  TAccountEmployer extends string = string,
  TAccountStream extends string = string,
  TAccountVault extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  employer: TransactionSigner<TAccountEmployer>;
  stream: Address<TAccountStream>;
  vault: Address<TAccountVault>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getCloseStreamInstruction<
  TAccountEmployer extends string,
  TAccountStream extends string,
  TAccountVault extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof CASCADE_PROGRAM_ADDRESS,
>(
  input: CloseStreamInput<
    TAccountEmployer,
    TAccountStream,
    TAccountVault,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress }
): CloseStreamInstruction<
  TProgramAddress,
  TAccountEmployer,
  TAccountStream,
  TAccountVault,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? CASCADE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    employer: { value: input.employer ?? null, isWritable: true },
    stream: { value: input.stream ?? null, isWritable: true },
    vault: { value: input.vault ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA' as Address<'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'>;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.employer),
      getAccountMeta(accounts.stream),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getCloseStreamInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseStreamInstruction<
    TProgramAddress,
    TAccountEmployer,
    TAccountStream,
    TAccountVault,
    TAccountTokenProgram
  >);
}

export type ParsedCloseStreamInstruction<
  TProgram extends string = typeof CASCADE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    employer: TAccountMetas[0];
    stream: TAccountMetas[1];
    vault: TAccountMetas[2];
    tokenProgram: TAccountMetas[3];
  };
  data: CloseStreamInstructionData;
};

export function parseCloseStreamInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedCloseStreamInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      employer: getNextAccount(),
      stream: getNextAccount(),
      vault: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getCloseStreamInstructionDataDecoder().decode(instruction.data),
  };
}
